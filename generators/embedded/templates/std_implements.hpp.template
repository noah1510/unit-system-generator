#pragma once

#ifndef UNIT_SYSTEM_UNIT_TYPE_TEMPLATE
#error "do not directly include the std workarounds!"
#endif

{%if target == 'arduino' %}

#include "Arduino.h"

#define UNIT_SYSTEM_SIZE_T unsigned long long

#define UNIT_SYSTEM_ABS(x) ((x) < -(x) ? -(x) : (x))

{%endif%} //end target_arduino
